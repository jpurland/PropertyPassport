# Cloudflare Worker: propertypassport
# Framework: Next.js static export (output: "export") → ./out
# Dashboard Worker name must match this `name` or the Git build fails.

name = "propertypassport"
compatibility_date = "2026-09-20"

[assets]
directory = "./out"
